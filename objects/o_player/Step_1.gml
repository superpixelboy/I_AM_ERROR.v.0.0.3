//Fire/Attack

/*
firingdelay = firingdelay -1;
//thisDirection = image_xscale;
if (mouse_check_button(mb_left)||keyboard_check(vk_lcontrol)||gamepad_button_check_pressed(0,gp_face3)) && (firingdelay <0)
{
	isAttacking = 1;
	firingdelay = 30;
	with (instance_create_layer(x,y,"Bullets",o_bullet))
	{
		speed = 8;
		
		if(other.image_xscale>=0)
		{
			direction = 0;
		}
		else{
			direction = 180;
		}
		
	}
}
*/
